# Testing
- Coverage reports are produced as `coverage/clover.xml` (single-site) and `coverage/clover-multisite.xml` (multisite); inspect `count="0"` lines in these files to locate uncovered statements. Confidence: 0.85
- Achieve 100% line test coverage with zero test failures for all modifications. Confidence: 0.94
- Use `@codeCoverageIgnore` annotations on intentionally-uncovered lines (e.g., multisite-only branches), but the annotation alone does NOT satisfy the coverage gate: `coverage-check.php` counts any clover `stmt` line with `count="0"` as a gap regardless of the annotation, so the line must also be listed in `coverage-exclude.json` to actually be skipped. Confidence: 0.85
- Test on both single-site and multisite WordPress configurations before committing test changes — the user explicitly probes whether test changes (e.g., capability-gate test setups) hold up on multisite, so run the affected suites in both environments. Confidence: 0.75
- Remove dead code (unused methods) when they are no longer called, and update tests accordingly. Confidence: 0.70
- Maintain `coverage-exclude.json` when triaging uncovered lines: add entries for lines that legitimately cannot be covered, and the file is consulted during coverage checks to skip those lines when measuring coverage completeness. Confidence: 0.70
- `coverage-exclude.json` line-number entries go stale as code shifts: an excluded line may no longer be an executable statement (e.g., OAuth_Connect.php:343 became a docblock `@return` line while the actual uncoveable multisite branch moved to 362). When a coverage summary flags a line, triage between a real gap and a stale exclusion by checking whether the listed line is still a statement in the current source, whether a passing test covers it under the right env (`WP_MULTISITE=1`), and whether the report is fresh — then update the entry to the current line number rather than leaving a dead entry. Confidence: 0.8
- After a merge or rebase, re-verify `coverage-exclude.json` line numbers against freshly generated clover output rather than trusting either branch's values: the rebased tree's line positions differ from both parents (e.g., Theme.php's multisite-only branch sat at 315 on dev and 316 on db; the rebased tree reported the uncovered line at 316). Run the coverage summary after the rebase, check the actual `count="0"` lines, and correct the exclusion to match. Confidence: 0.7
- When checking test coverage, examine the existing `coverage/clover.xml` and `coverage/clover-multisite.xml` reports first before rerunning tests; only regenerate coverage if existing reports already show 100% line coverage, since code edits may have introduced uncovered lines. Confidence: 0.85
- The `wp-env-macos test` wrapper does not forward extra PHPUnit args (`--filter`, `--group`, etc. are dropped — the `test` dispatch only passes `$2` as the mode); to run a filtered subset, exec phpunit directly in the tests-cli container, e.g. `wp-env-macos exec tests-cli sh -c 'cd /var/www/html/wp-content/plugins/git-updater && WP_MULTISITE=1 WP_TESTS_PHPUNIT_POLYFILLS_PATH=/var/www/html/wp-content/plugins/git-updater/vendor/yoast/phpunit-polyfills vendor/bin/phpunit --config=phpunit.xml --filter <test>'`. Confidence: 0.8
- Back up every file in fixture directories (not just style.css) when a test modifies bind-mounted theme files during upgrade tests; restore all files in tear_down() so the host working tree is left unchanged. Confidence: 0.80
- When code behavior changes (committed or uncommitted), update existing tests that assert the old behavior to assert the new behavior, and add new coverage for the changed edge cases (e.g., dev release asset version differing from vs. matching the remote version). The user asks to "fix and update tests for current uncommitted changes" expecting the assistant to inspect the working tree and align the suite with it. Confidence: 0.75
- Test email-sending behavior by capturing `wp_mail` with a filter (must return `true` to suppress actual sending), asserting against `get_option('admin_email')` rather than a hardcoded address, and cleaning up with `remove_all_filters('wp_mail')` in `tear_down()`. Confidence: 0.75
- Stub Freemius in tests by swapping `$GLOBALS['gu_fs']` with an anonymous class exposing only the methods under test (e.g., `can_use_premium_code()`), and restore it in `tear_down()`. Confidence: 0.75
- When tests assert state in a shared database table (e.g., a repo cache table), clean that state in BOTH `set_up()` and `tear_down()`: `set_up()` must delete any stale rows a previous test class left behind (a filtered run failed with a leftover `'db'` current_branch row from earlier cache-table tests until `delete_repo` was added to `set_up()`), and `tear_down()` must not leak rows forward. Test classes must be order-independent. Confidence: 0.8
- When migrating data from an option to a database table, update the affected tests to assert BOTH that the new storage location holds the value AND that the old location no longer does (e.g., assert `isset( $option['current_branch_git-updater'] )` is false while the cache table row is `'develop'`), so the migration's completeness is locked in. Confidence: 0.75
- The test environment runs on Apple's `container` and `opossum` runtimes, set up and driven by `wp-env-macos` (local binary `./node_modules/.bin/wp-env-macos test ...`) — not Docker; the user explicitly and repeatedly reminded to remember this setup (stated again as "This project uses wp-env-macos, Apple container, and opossum as a Docker substitute"). Confidence: 0.95
- When a decision method's input condition changes (e.g., `use_release_asset()` switching from `newest_tag` to `release_assets`, or `waiting_for_background_update()` switching from `empty($cache)` to a complete `ran` key), update every test of that method — and every downstream test that seeds caches relying on the old semantics (e.g., plugin/theme `plugins_api`/`themes_api` filter tests) — to set the new condition, convert the replaced-condition tests into coverage for the new behavior, and add new edge-case tests (e.g., partial `ran` → waiting; cached data without `ran` → waiting). Confidence: 0.75
- In WordPress multisite, a plain `administrator` role does NOT have `install_plugins`/`install_themes` capabilities — only super admins do, so capability-gate tests running as an admin must call `grant_super_admin( $admin_id )` in `set_up()` (a no-op on single-site, so the same test passes both environments); a negative capability test that switches the current user to a subscriber is unaffected by the persisted grant. Confidence: 0.8
- When adding a capability check to a settings/save handler, update the affected test classes' `set_up()` to create an administrator user and `wp_set_current_user()` it (and reset to 0 in `tear_down()`), otherwise the new check makes existing save-path tests fail with no logged-in user. Confidence: 0.7
- When a `coverage:summary` run appears truncated (e.g., it prints the single-site result and stops without the multisite result), the multisite run may have failed silently — exec the multisite suite directly (`wp-env-macos exec tests-cli sh -c '... WP_MULTISITE=1 ... vendor/bin/phpunit --config=phpunit.xml'`) to surface the actual errors before trusting the summary or triaging coverage entries. Confidence: 0.7
/`themes_api` filter tests) — to set the new condition, convert the replaced-condition tests into coverage for the new behavior, and add new edge-case tests (e.g., partial `ran` → waiting; cached data without `ran` → waiting). Confidence: 0.75
s (e.g., partial `ran` → waiting; cached data without `ran` → waiting). Confidence: 0.75
partial `ran` → waiting; cached data without `ran` → waiting). Confidence: 0.75
s (e.g., partial `ran` → waiting; cached data without `ran` → waiting). Confidence: 0.75
