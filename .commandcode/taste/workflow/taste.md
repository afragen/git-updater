# Workflow
- When planning a user-triggered per-item action (e.g., a flush-repo-cache button), consider the repeated-interaction scenario — the user probes "consider what happens if the user clicks several of the links" — expecting the design to handle rapid consecutive clicks gracefully (batched/consolidated behavior on the next page load) rather than race-prone per-click scheduling, and to document why the simple design already handles it. Confidence: 0.65
- When reverting a feature that only ever existed in `[unreleased]` (never shipped), remove its original `[unreleased]` changelog entry rather than adding a new entry describing the revert — the user directive ("Maybe just remove the changelog entry describing the AJAX handler in [unreleased]") means the feature simply never appears in any changelog. Confidence: 0.7
- Do NOT do feature/fix development in git worktrees: a worktree lacks the gitignored initialization (`vendor/`, `node_modules`, `compose.yaml`, `.env`) that wp-env-macos needs, so running tooling/tests from a worktree breaks (empty `vendor/bin`, missing compose stack, symlinked binaries resolving against the wrong CWD, port collisions with the main checkout's running env). Instead, create the feature/fix branch FROM the appropriate target branch (e.g., `develop`) in the main checkout, where all wp-env-macos initialization has already been done, and do the work there. The user explicitly corrected the worktree approach: "This is an issue you created by running from a worktree and not creating a new feature/fix branch from the appropriate branch and working there where all initialization from wp-env-macos has been done." Confidence: 0.85
- The user actively reads and audits the `.commandcode/taste/` files and reports corruption (e.g., "some of the changes to .commandcode/taste/testing/taste.md have been corrupted, especially from line 24 to the end") when appended learnings arrive as truncated fragments: partial duplicate lines missing the leading `- ` bullet marker, leftover tails of an earlier bullet, or duplicated section headings. Keep taste files clean — every learning must be written as a complete `- ` bullet, never a fragment — and when corruption is reported, repair it across all taste files (remove orphan fragments and duplicated sections while preserving the legitimate new bullets), even when some of the corruption was committed in earlier commits. Confidence: 0.6
- Commit changes after completing each distinct work phase. Confidence: 0.90
- Do not publish packages to npm; leave publishing for the user to handle manually. Confidence: 0.65
- Run tests before committing changes to verify nothing is broken. Confidence: 0.80
- Accept as fact that any branch based off `master` or `develop` has all its testing setup already provisioned via wp-env-macos (Apple container + opossum) and will run appropriately — when on or based on a long-lived branch, run the suite directly with `wp-env-macos test` / `test:multisite` rather than re-provisioning or doubting the environment (e.g., the user confirmed the full single-site and multisite suites run cleanly). Confidence: 0.7
- Use tiered approach: address High priority issues first, then Medium, then Low. When the user asks to create a plan fixing a batch of identified issues, rank the fixes by order of importance/impact within the plan — the user explicitly asked to "rank them by order of importance." Confidence: 0.82
- When a fix or plan touches an API/service layer that a consuming client calls (e.g., an OAuth connector relaying token requests to a provider), plan the coordinated change on the consuming client side as well — the provider is often a stateless relay while the client owns the state (tokens, revocation decisions), so a provider-side fix alone changes nothing on the client. The user's plan comments asked how each connector change "lands in" the consuming git-updater plugin, revealing they expect downstream/consumer impact traced for every fix. Confidence: 0.6
- When taste files are modified, omit them from any commit rather than reverting them with git checkout. Confidence: 0.70
- Taste files are always committed ONLY together with other taste files, and ONLY to the develop branch — a commit containing taste file changes must not also include code/test changes, and must never be made to master or other branches. The user stated this as an explicit rule: "taste file must not be committed with code. They are only to be committed to the develop branch with other taste files." This overrides any prior repo-dependent behavior of bundling taste files into code commits or committing them to non-develop branches. Confidence: 0.98
- Commit taste files to the `develop` branch specifically (not `master`), even when the working tree is on `master` — this is a recurring rule for this repo, and when the two branches' taste versions have diverged, merge/reconcile the working tree's new learnings onto `develop`'s richer version rather than clobbering or committing to master. Confidence: 0.8
- Create a CHANGES.md entry before committing, unless the user explicitly specifies to commit without changelog entries. The changelog entry must be staged and included in the same commit as the code change — the user audits commits for it and prompts tersely when it is missing from a commit ("did you forget the changelog entry?" when an amend omitted CHANGES.md), so verify the commit contents (e.g., `git show --stat HEAD`) include the changelog alongside the code/tests. Confidence: 0.95
- Add changelog entries to the END of the `[unreleased]` section of CHANGES.md (append, not prepend). The user explicitly reminded the assistant to place them at the end after the assistant had put them at the top. Confidence: 0.90
- Document new user-visible behavior with a concise `[unreleased]` bullet in CHANGES.md (single line, lowercase, covering the change; e.g., email notification + reminder cadence). Confidence: 0.75
- Verify a plan before implementation: the user reviews plans carefully and points out logic flaws (e.g., a dedupe guard that can never trigger because the flag is always cleared first) as well as planned steps that existing mechanisms already handle — e.g., a manual one-time `_error`-row migration that `prune_stale()` already performs on every admin load made that step unnecessary — so re-check the proposed changes against the actual code paths before finalizing and drop redundant steps. Confidence: 0.82
- Keep plan documents under `~/.commandcode/plans/` current with the latest understanding: when a follow-up discussion clarifies or corrects the semantics after a plan was written — even after implementation is done and verified — update the plan to document the refined model (e.g., the "managed-ness comes from the repo object/config, never the cache" model for `waiting_for_background_update()`, including the `gu_additions` path) rather than leaving it describing the superseded framing. The user prompts this with terse questions like "Do we need to update the plan for waiting_for_background_update()?" and approves the documentation-only revision without code changes. Confidence: 0.7
- CHANGES.md entries should be concise — a short single-line summary of the change rather than a long description. Confidence: 0.80
- Periodically review and trim the `[unreleased]` section in CHANGES.md to remove entries that no longer reflect the current state of the code — verify each entry against the actual codebase (search for the methods/features mentioned, check git history) and remove entries whose behavior has been superseded, reverted, or replaced by subsequent changes. Apply this review to both long-lived branches (master and develop) since they may have different entry sets. Confidence: 0.75
- Add new changelog entries after the last existing entry in the `[unreleased]` section (before the next version header), not at the top of the section — the user explicitly corrected this: "It's better to add new changelog entries after the last entry in [unreleased]." Reinforced again when the assistant added entries at the top and was told "you ignored previous instructions to add changelog entries to the bottom of the [unreleased] section." Confidence: 0.98
- Use Apple Container and opossum via wp-env-macos for container operations instead of running Docker directly. Confidence: 0.95
- The `wp-env-macos` wrapper is a real git repo the user maintains separately at `~/Documents/github/wp-env-macos` (clean on `main`), NOT the installed `node_modules/wp-env-macos` package copy. When a fix or change to the wrapper is needed, investigate and edit the source in the standalone repo (e.g., `~/Documents/github/wp-env-macos/bin/mac-env`), not the git-ignored `node_modules`/`.bin` shims. The user redirects the assistant to this location tersely when it analyzes the wrong copy: "wp-env-macos is at ~/Documents/github/wp-env-macos". Confidence: 0.7
- The wp-env binary was renamed from `wp-env-opossum` to `wp-env-macos` — the old name no longer exists; use `./node_modules/.bin/wp-env-macos` for both tests (`test`) and coverage checks (`coverage:check --exclude coverage-exclude.json`). The user corrected the assistant when it looked for the renamed binary. Confidence: 0.85
- When RUNNING tests through wp-env-macos, use the local package copy (`node_modules/.bin/wp-env-macos`, or `npx wp-env-macos test`) — do NOT invoke the standalone source repo binary (`~/Documents/github/wp-env-macos/bin/mac-env`) while running, even though that separate repo is nevertheless the place wrapper source changes are edited. The user explicitly directed "use the local copy of wp-env-macos in node_modules" after the assistant ran `mac-env ps` from the source repo. Confidence: 0.80
- When an in-progress change keeps failing in confusing ways (e.g., an architectural change causing cascading test failures whose mechanism is unclear), the user prefers to abandon the approach entirely rather than continue debugging — the terse directive "stop work on current process and revert from both develop and master" means: revert all uncommitted changes on every affected branch to the last committed state, delete empty hotfix branches created for the work, verify no debug leftovers remain, and preserve taste files (stash/pop as needed) — do not partially keep pieces of the reverted work or keep investigating the failed approach. Confidence: 0.70
- Stash dirty `.commandcode/taste/` files BEFORE switching branches for hotfix work: a dirty taste file aborts `git checkout <branch>`, and a subsequent `git checkout -b <new-branch>` then silently creates the new branch from the still-current (wrong) HEAD (e.g., develop instead of master). Verify the new branch's base commit (`git log --oneline -1`) before editing, and delete any mis-rooted branch before recreating it from the correct parent. Confidence: 0.65
- When the same bug affects multiple satellite plugin repos with an identical root cause, apply the same fix uniformly across every affected repo, and commit each repo separately using the same commit message (e.g., loop over the repos). Confidence: 0.80
- After a fix lands in the main repo's shared trait/logic (e.g., `use_release_asset()` in GU_Trait, inherited by all host API classes), audit each satellite API repo's parallel `construct_download_link()` and apply the fix only where the identical defect exists: Gitea had the same `return ''` dead-link pattern and was fixed; GitLab (CI-job artifact fallback) and Bitbucket (returns a link or falsy) needed no change; Bitbucket Server has no release-asset block. Verify each host's return shape before changing it. Confidence: 0.7
- Write commit messages with a concise summary line and a `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer; for larger changes include a body paragraph explaining the root cause and a bulleted list of the changes — small, focused fixes can be a one-line summary plus the trailer. Confidence: 0.70
- Once the root cause is understood and a fix is identified, proceed to implementation promptly rather than continuing to investigate; the user nudges when analysis drags on or when nothing visibly happens (e.g., an interrupted file write leaves no deliverable on disk). Confidence: 0.82
- After adding or changing a Composer dependency in `composer.json`, also update `composer.lock` (e.g., `composer update <package> --with-dependencies --no-interaction`) so the committed lock file stays in sync and CI installs the new dependency. Confidence: 0.65
- In the API plugin repos (e.g., git-updater-bitbucket, git-updater-gitea, git-updater-gitlab), `.gitignore` should use an allowlist approach for `vendor/`: ignore everything except the minimal paths needed for CI autoloading — track only `vendor/composer` and `vendor/autoload.php` (`vendor/bin` and `vendor/afragen/singleton` are NOT tracked). Do not add per-package ignore entries for each installed dev dependency. The user explicitly rejected a plan that would fully gitignore `vendor/` for these repos — the committed Composer autoloader must stay there. (git-updater-gist is an exception: it fully gitignores `/vendor/` and untracks it, because it uses a custom PSR-4 autoloader instead of the committed Composer autoloader.) Confidence: 0.85
- For git-updater-gist, which uses a custom PSR-4 autoloader (`autoloader.php`) that loads the plugin's own `src/` classes and loads `vendor/autoload.php` only when present, `vendor/` is fully gitignored (`/vendor/`) and untracked via `git rm -r --cached`; the committed Composer autoloader is not needed. The user approved this approach. Confidence: 0.70
- Use the current stable major version of GitHub Actions in workflow files and plans (e.g., `actions/checkout@v7`, not `@v4`); the user tracks latest action releases and corrected a plan that used an outdated version. Verify the current release when unsure. Confidence: 0.80
- When asked to execute an already-approved plan saved under `~/.commandcode/plans/`, proceed directly to implementation — do not enter plan mode and do not re-present the plan via exit_plan_mode; read the plan file first, then implement it step by step. The user repeatedly issues the terse directive "implement plan" for this. Confidence: 0.85
- When the user configures the tool via settings.json (e.g., `permissions.additionalDirectories`), they want an observable way to verify the configuration is actually applied — a command that displays current state, or a concrete behavioral test — rather than taking the settings at face value; explain both the mechanism and how to confirm it. The user will actively test any suggested verification command and report back if it does not reflect the configured state, so verify against the source that the command actually surfaces the state before recommending it (e.g., `/status` and `/add-dir` do not display settings-configured dirs). Confidence: 0.80
- When a built-in Command Code command lacks a feature they need (e.g., no command displays settings-configured `additionalDirectories`), the user prefers to extend the tool itself by building a custom slash command via the official mod system (mod-builder skill) rather than working around the gap externally. Confidence: 0.60
- After a file write that was supposed to create a deliverable (e.g., a mod file), verify it actually landed on disk (e.g., `ls` the target directory) rather than assuming success — interrupted writes can silently fail and the user will report that "nothing is happening." Confidence: 0.70
- Missing Docker does not mean the test suite cannot run — this project's tests run via `wp-env-macos` (Apple container + opossum), not Docker. Run the fast local checks first (`vendor/bin/phpcs`, `vendor/bin/phpstan analyse` on changed files, `php -l`), then check the environment is up (`wp-env-macos ps`, e.g., `tests-cli` running) and run the suite with `npx wp-env-macos test` (and `test:multisite` for the multisite suite) rather than concluding the suite is unrunnable. Confidence: 0.65
- Tests are ALWAYS run via `wp-env-macos` (`npx wp-env-macos test`) — never invoke the local `vendor/bin/phpunit` binary directly, since the WP test library is not installed in the local environment and the suite only runs inside the wp-env-macos container. This is a repeated, emphatic correction: the user has had to remind the assistant ("how many times must I tell you that testing is run via wp_env_macos??", "How many times do I need to tell you phpunit tests are run via wp-env-macos and not wp-env?") after it tried `vendor/bin/phpunit` or reached for the wrong env tool. Reach for `wp-env-macos` first, every time. Confidence: 0.92
- When asked to "check <method> with the preceding changes" (e.g., "Check get_repo_plugin_meta() with the preceding changes") after a code change, trace how the changed helper interacts with every caller — including the failure path — and report any behavioral regressions the change introduces (e.g., `waiting_for_background_update()`'s `ran` check means a persistently-failing repo is now queued to cron forever and blocks `unset_stale_options()`), rather than just confirming the tests pass. Confidence: 0.7
- Do not make code changes beyond the explicit request: when the user declines an options question about whether to plan a fix (e.g., a retry guard for a regression found during a check), leave the code as-is and surface the finding in the summary for a later decision rather than proceeding with the recommended change unrequested. Confidence: 0.6
- Before attributing an unexpected change in test count to a code edit, verify which branch the working directory is on (`git branch --show-current`, `git log --oneline -3`): this repo has multiple long-lived branches (e.g., `db` vs `develop`) and the session can switch branches between tasks, so an older branch legitimately has fewer tests (e.g., 1416 vs 1500) and the difference is not a regression. Confirm where the change was actually made and flag branch mismatches to the user rather than treating the count drop as a failure. Confidence: 0.7
- Git workflow: releases are cut from `develop` into `main` (update `develop` for the release, merge into `main`), and feature branches — e.g., `db` for the tables feature — are merged into `develop` only after the release is complete. The user explicitly plans this ordering so release content is not held up by in-progress feature work. Confidence: 0.75
- `wp-env-macos` has no `status` subcommand (the wrapper drives opossum, which lacks one), but it does have `ps` — use `wp-env-macos ps` to see whether the test environment is up. Do NOT probe docker or podman: the user explicitly corrected that the containerized test environment runs in wp-env-macos using Apple container and opossum and does not run in docker or Podman, so checking `docker ps` or starting a `podman machine` is the wrong path before concluding the suite is unrunnable. Confidence: 0.8
- Write implementation plans with a trailing Verification section listing the concrete commands to run after the changes (e.g., `vendor/bin/phpcs` on the changed files, `vendor/bin/phpstan analyse`, and the affected test suite), so implementation ends with explicit verification rather than ad-hoc checking. Confidence: 0.55
- When a plan's verification step proposes adding temporary debug instrumentation or a debug counter in a dev branch to measure behavior (e.g., confirming an object-cache hit rate or a reduced DB-query count), the user challenges it with "Is this plan the best way to accomplish this?" and prefers non-invasive measurement instead: use built-in config-flag instrumentation (e.g., WordPress's `define('SAVEQUERIES', true)`) or passive observation of real HTTP traffic via a one-off mu-plugin/filter, plus an automated test that permanently proves the behavior — so no debug code ships and no dev branch is polluted. Confidence: 0.7
- When planning changes, verify the plan's assumptions (touched methods, code shapes) hold on both long-lived branches (master vs develop) — the branches differ substantially (e.g., GU_Trait differed by ~273 lines) — since the user asks "will this work on master and develop" and expects branch-aware planning. Confidence: 0.65
- When a change must land on both long-lived branches (master and develop), create a dedicated hotfix branch carrying the change so it can be applied to both branches (e.g., merged/cherry-picked into each), rather than committing it directly to a single branch and duplicating the work. The user explicitly stated: "I wish to create a hotfix branch with this change so that I can apply it to both 'master' and 'develop' branches," then followed through — created `hotfix-release-asset-flow` based off `master` and announced the branch state ("Current branch is hotfix-release-asset-flow based off master") before implementation, expecting the assistant to verify the branch base (e.g., via `git merge-base`) and plan the propagation into both branches. Confidence: 0.8
- The two long-lived branches differ in storage architecture, not just code: `master` has no `src/Git_Updater/DB/` cache table — repo data is cached in `ghu-<md5>` site options and `get_repo_cache( $repo, $timeout )` returns the whole array; `develop` uses the `git_updater_cache` DB table where `get_repo_cache( $repo, $timeout, $column )` projects a whitelisted column (schema changes require the whitelist + `db_version` bump). When a fix must land on both branches and depends on the cache, verify which mechanism each branch uses and create TWO separate hotfix branches — one off each parent (e.g., `hotfix-transient-release-asset-master` / `-develop`) — sharing the same consumer logic but differing in persistence (option key vs DB column); merge each into its parent with `git merge --no-ff` and re-run the full suite on the merged branch. The user explicitly requested the two-branch split over one branch applied to both. Confidence: 0.8
- When a hotfix must land on multiple branches and `.commandcode/taste/` files are dirty, keep them out of the code commit by stashing them before committing, perform the merges, then pop the stash in the develop branch and commit the taste files there in their own separate commit — the user proposed this exact sequence as the simpler approach ("It might be simpler to stash the taste files and after the merges, pop the stash in the develop branch and commit the taste files to the develop branch"), keeping taste-file commits off master and confined to develop. The user later generalized this into an explicit rule: since taste files are only committed to develop, a stash holding taste files should only ever be popped on develop ("since we only commit taste files to develop branch we should only pop the stash of taste files on develop"). When popping the stash conflicts because the target branch's taste files diverged, resolve by keeping both sides, then drop the stash entry once everything is staged. Do not disturb the user's pre-existing stash entries (e.g., `stash@{0}: On develop: WIP on develop`) — drop only the stash entry created for the taste files. Confidence: 0.75
- When the user asks "When was <method> introduced and why?" about a specific method, they expect full git-history forensics, not a guess: locate the introducing commit with `git log --all -S "<name>" -- '*.php'`, inspect that commit's metadata (date, author, branch/merge context) and its file diff plus the CHANGES.md entry, then explain both the when (exact commit hash + date + how it reached the mainline, e.g., via a feature-branch merge) and the why (the concrete bug or missing feature it addressed — e.g., tag rollbacks were downloading the latest release asset instead of the target tag's), including deliberate design decisions documented in the code (e.g., tag-specific release-asset lookups intentionally NOT cached so they cannot overwrite the primary cache entry, `construct_download_link()` only writing the cache for non-tag targets) and how the method fits the current call graph (visibility, sole caller, tests added alongside). Confidence: 0.6
- When the user issues a terse directive to REVERT a feature and simplify to a base model (e.g., "create a plan to revert the changes associated with get_release_asset_for_tag() and only be concerned about saving and the current release asset or the current dev release asset"), keep the plan a faithful revert/simplification — do not unilaterally pivot it into a larger refactor that preserves part of the feature (e.g., rewriting the plan into a tag-matching refactor that kept `get_release_asset_for_tag()`); a plan that drifted from the explicit revert instruction was not approved, and the assistant had to ask "The plan was not approved. What changes would you like me to make to the plan?" Confidence: 0.55
- When the user rejects a plan and supplies per-line comments on the saved plan file, they expect EVERY comment addressed by updating the plan in place at the same path and re-presenting it via exit_plan_mode — an explicit instruction like "Address every comment and update the plan file in place" overrides any earlier "stop and wait," and "Do not start implementing" means the loop stays plan-only until approval. Confidence: 0.7
- When the user asks to review a plan created in a previous session (e.g., "review last plan"), the `plan_review` tool will not find it (it only recognizes plans registered in the current session): locate the newest file under `~/.commandcode/plans/` (e.g., `ls -lt`), read it, re-register it by rewriting the file (`write_file` with the same content), then open `plan_review` again. Confidence: 0.65
- Multi-repo plans must state the branch scope explicitly for every repo, consistently across ALL plan sections (goal, per-repo change tables, files-to-change, order of work, verification, changelog) — the user corrected a plan where the scope appeared only in some table rows. In this ecosystem, feature changes in the satellite API plugin repos land only on each plugin's `develop` branch (plugin master branches stay untouched), while core git-updater changes are applied to both master and develop. Confidence: 0.7
- In the git-updater repo, when the user requests a code change, apply it to both `master` and `develop` branches (using git stash to switch between them) and verify with PHPStan on each branch, unless the user specifies otherwise. Confidence: 0.85
- When applying changes to multiple branches (master and develop), follow this specific sequence to avoid repetition and lost work: (1) complete all code changes on the first branch, (2) run tests and verify with PHPStan, (3) add CHANGES.md entry, (4) stash the completed changes with a descriptive message, (5) switch to the second branch, (6) make the same/similar changes there, (7) test and verify, (8) stash those changes, (9) switch back to the first branch, (10) pop the stash, (11) ask the user to commit, (12) repeat steps 5-11 for the second branch. The user explicitly corrected this workflow after the assistant kept forgetting to stash branch-specific changes before switching, causing repeated work. Confidence: 0.9
- Use a dedicated temporary feature branch for making and testing changes, then merge it into the target branch (master/develop) only after all verification and validation passes, and delete the temporary branch as cleanup afterwards — rather than working directly on a protected branch or a throwaway worktree. The user explicitly asked "why aren't you creating a new git branch for creating and testing changes... then merge in that branch once all verification and validation is complete and clean up by removing that new branch." Confidence: 0.95
- Never delete or remove the `develop`, `main`, or `master` branches of any project — they are protected, long-lived branches. Stated explicitly: "Never remove/delete the develop, main, or master branches of any project." Confidence: 0.95
- The user keeps track of what has been committed to each long-lived target branch (master vs develop) and will ask terse confirmation questions like "wasn't this already committed to master?" when a fix appears to be re-done. Answer by confirming the actual state with `git log` on the branch in question, and clearly explain how the two targets differ (e.g., the same fix needs a branch-specific implementation on develop) rather than asserting from memory — master and develop can legitimately be at different states for the same logical change. Confidence: 0.65
- To test an unpublished change to the wp-env-macos package in a consuming workspace, follow the package README's documented "Local development (testing an unpublished change)" flow (README.md lines 347-367): Option A `npm install -D /abs/path/to/wp-env-macos`, or Option B `npm link` (preferred fast-path when actively editing, since changes propagate live). The consumption mechanism for a fix inside the repo is the local-link/install step, not copying files into a consumer's `node_modules`. Confidence: 0.60
- Prefer surgical, targeted dependency fixes over destroying `package-lock.json` + `node_modules`: when a linked/local-path dependency isn't picked up (e.g., a stale `file:` lockfile pin), re-resolve just the affected dependency (`npm install` to reconcile the lockfile, or a targeted `npm install -D <local-path>`) rather than deleting the whole lockfile and node_modules, which loses resolution pins for all deps. The user challenged "remove package-lock.json + node_modules and reinstall" as over-broad, asking whether that heavy step is really the best plan. Confidence: 0.60
- The main git-updater repo maintains parallel commits across `master` and `develop` (mirrored feature commits, e.g., `165695aa`/`2d249f0b`, `038c1319`/`57fcde84`). When a fix/feature lands in the main repo, the plan and implementation must cover BOTH branches: apply the same source and test changes on each, commit separately per branch, and verify each independently (repeat the full verification on `develop` and confirm `master` isn't regressed). The user flagged this in plan review when a plan initially omitted the second branch. Confidence: 0.70
- When a change spans a small, coherent set of files (e.g., a single `OAuth_Connect.php`) and the two long-lived branches have diverged heavily (e.g., 121 vs 41 commits), prefer **cherry-picking the one commit onto the second branch** over performing a full `master → develop` merge: the full merge drags in the unrelated divergence and produces many conflicts in files you did not touch, whereas the specific file often auto-merges cleanly on cherry-pick (git applies the change onto the divergent version). Abort the conflicted merge and switch to the cherry-pick rather than resolving unrelated conflicts. Verify the new logic is present on both branches and re-run the test/static checks on each branch's variant. Confidence: 0.65
- When a plan is under review, the user runs iterative hardening rounds, opening each with a terse question soliciting criticism/improvements (e.g., "are there any issues, weaknesses, or flaws in this plan", "are there any additional improvements to make with the plan?"). They expect the assistant to proactively surface the REAL design gaps (not nitpicks) by verifying each claim against the actual source before folding the correction in, and to update the plan in place at the same path and re-present it via exit_plan_mode after each round — repeating until the user is satisfied. The user keeps pushing for more rounds even after the plan looks complete. Confidence: 0.75
- When the user asks "are there any issues, weaknesses, or flaws in this plan" after the assistant writes a plan, they want an honest, critical self-review that surfaces the REAL flaws and concrete design gaps (not nitpicks) — e.g., a computed value that's impossible as written, a signature mismatch, an implementation claim that doesn't actually hold at the call site, or a logic path that does something other than what the plan says. The review should be delivered as a critique and the assistant should offer to fold the corrections into the plan, not silently edit it, since the user framed it as a review question. Confidence: 0.70
- When the user responds to a plan review by directing "re-verify and fold corrections into the plan," they expect the assistant to first re-verify each flagged flaw against the actual source code (e.g., confirming `recordFailedAttempt()` really stores only the count with no window start-time, or that the client already has a per-provider refresh lock) before applying the corresponding correction — not to fold changes in blindly based on the earlier critique — then update the plan in place at the same path and re-present it via exit_plan_mode. Confidence: 0.75
- When the user points out that source plugins live in a local site (e.g., `/Users/afragen/Local_Sites/lightning/app/public/wp-content/plugins/`) and explicitly instructs "Do not delete anything in <site dir>", treat that local-site installation directory as protected/non-destructive for the task at hand — read/search from it but never delete or modify its files, since the user guards against destructive operations on the local environment. Confidence: 0.8
- When committing, first inspect the working tree (`git status`, `git diff`) to separate the task's changes from unrelated in-progress changes the user has in the tree (e.g., a `package.json` dependency bump, extra edits in a file you touched, or `.commandcode/taste/` changes) and only stage/commit the changes belonging to the task, leaving the others untouched for the user to commit separately. When the user's unrelated changes are entangled in the same file you edited such that they can't be cleanly separated by path, ask the user which scope to include in the commit (e.g., via ask_user_question) rather than bundling everything or guessing. Confidence: 0.7
